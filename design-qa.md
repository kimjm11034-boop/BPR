# Design QA

final result: passed

## Scope

- Reference: 사용자 첨부 모바일 시안 (`codex-clipboard-087359d5-c76b-4d1a-aed5-91c52c268385.png`) 및 선택된 3번 시안
- Runtime: local Next.js app at the 390 × 844 mobile viewport
- Compared states: 경기 준비 + 등록 선수 전적, 명단 확정 후 경기 입력 + 오늘 기록, 전체 누적 기록, 개인 순위, 파트너 조합

## Verified

- 초록색 배드민턴 코트 이미지가 전체 배경으로 깔리고, 흰색 입체 카드와 라임색 주요 CTA가 시안의 계층을 유지한다.
- 시작 화면에서 경기 일자를 입력하고 4명 이상의 선수 명단을 추가·삭제할 수 있으며, `명단 확정하고 경기 시작`을 누르면 경기 입력 화면으로 이동한다.
- 확정한 명단이 경기 입력 화면의 복식 선수 선택 목록에 즉시 반영되고, 동명이인은 별도 ID로 유지된다.
- 홈의 등록 선수 목록에 최근 경기 수·승패·승률을 표시하고, 선수 삭제 시 경기 선택 목록과 순위표에서만 제외한다.
- 삭제한 선수의 기존 경기 기록은 전체 누적 기록에 남는다.
- 홈의 명단 확정 CTA를 제거하고, 하단 `경기입력` 메뉴에서 등록 선수 명단을 바로 사용하도록 변경했다.
- 배포용 clean-slate 데이터 버전으로 기존 누적 경기와 오늘 기록을 초기화하고, 빈 전체 기록 상태를 확인했다.
- 앱 이름을 BPR로 변경하고 제공된 라켓·셔틀콕 이미지를 192px/512px PWA 아이콘과 Apple 아이콘으로 적용했다.
- 홈 화면에는 오늘 기록 카드가 노출되지 않고, 전체 기록 화면에는 누적 기록만 노출된다.
- 경기 입력 화면에서만 오늘 기록과 `초기화` 버튼을 제공하며, 초기화 후에도 전체 기록과 순위표의 누적 데이터는 유지된다.
- 날짜를 변경해 경기 세션을 시작하면 이전 날짜의 오늘 기록은 자동으로 비워지고 누적 기록은 유지된다. 앱을 켜 둔 상태에서도 날짜 변경을 주기적으로 감지한다.
- 오늘 경기 기록에서 승리팀 선수 두 명이 둥근 연두색 박스로 묶이고 왼쪽 위 `승` 배지가 표시된다.
- 네 선수 선택 전 저장 버튼이 비활성화되고, 같은 선수는 다른 자리에서 선택할 수 없다.
- 기록 저장 후 전체 경기 기록과 오늘 기록에 즉시 한 경기가 추가된다.
- 개인 순위와 파트너 조합 탭이 작동하며, 파트너 조합은 1~2경기에 `경기 수 적음`을 표시한다.
- 하단 내비게이션에서 경기입력·전체기록·순위표 화면이 전환된다.

## Follow-up polish

- 실제 Supabase 데이터를 연결하면 데모 localStorage를 읽기 쿼리와 Realtime refetch로 교체한다.
- 운영 배포 전 모바일 아이콘은 코트 사진 복사본 대신 별도 브랜드 아이콘 자산으로 교체한다.
